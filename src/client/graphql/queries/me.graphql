query QueryMe {
  me {
    id
    username
    phone
    defaultAccount {
      __typename
      id
      defaultWalletId
      wallets {
        __typename
        id
        balance
        walletCurrency
      }
    }
  }
}
